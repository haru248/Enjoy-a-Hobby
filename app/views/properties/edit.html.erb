<% content_for(:title, t('.title')) %>
<% breadcrumb :property_edit, @inventory_list, @property %>

<%= render 'property_form', { inventory_list: @inventory_list, property: @property, title: t('.title') } %>
